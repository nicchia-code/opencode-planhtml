{
  "name": "opencode-plan-agents",
  "version": "0.2.0",
  "type": "module",
  "packageManager": "bun@1.3.11",
  "description": "GitHub-installable OpenCode agent pack that adds ASK and overrides PLAN without patching core.",
  "bin": {
    "opencode-plan-agents": "./bin/opencode-plan-agents.mjs"
  },
  "scripts": {
    "smoke:installer": "node ./scripts/smoke-opencode-plan-agents-installer.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
